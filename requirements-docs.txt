# Docs site dependencies (mkdocs + i18n + material theme).
# Install with:  pip install -r docs/requirements-docs.txt
# Build locally:  mkdocs build           → site/   (English default)
#                 mkdocs build --config-file mkdocs.zh.yml   → site/ (Chinese)
# Or use the convenience script:         scripts/build-docs.sh
#
# These are NOT needed for the backend service or the Electron app;
# they only build the static documentation site for GitHub Pages.

mkdocs==1.6.1
mkdocs-material==9.6.14
mkdocs-static-i18n==1.3.0
pymdown-extensions==10.16